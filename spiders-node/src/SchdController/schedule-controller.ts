import { ScheduleService } from "../SchServices/schedule-service";

export class ScheduleController {
    ser = new ScheduleService();
    matchSchd = this.ser.matchSchd

   

}
 // score = this.ser.score;
    // id = this.ser.id;
    // id = 786;